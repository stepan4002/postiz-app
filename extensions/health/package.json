{
  "name": "@social/health",
  "version": "1.0.0",
  "description": "Health check endpoints for Social Command Centre: /health/live and /health/ready with DB, Redis, MinIO indicators",
  "main": "src/index.ts",
  "license": "AGPL-3.0",
  "private": true,
  "dependencies": {
    "@nestjs/common": "^10.0.2",
    "@nestjs/core": "^10.0.2",
    "@nestjs/terminus": "^10.0.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "jest --config jest.config.ts"
  }
}
