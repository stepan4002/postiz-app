---
phase: 04-media-library-processing
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - extensions/media-library/src/media-library.module.ts
  - extensions/media-library/src/index.ts
  - apps/backend/src/app.module.ts
  - libraries/react-shared-libraries/src/helpers/uppy.upload.ts
  - apps/frontend/src/components/media/company-media-library.tsx
  - apps/frontend/src/components/media/media-grid.tsx
  - apps/frontend/src/components/media/media-upload-button.tsx
  - apps/frontend/src/components/media/hooks/use-company-media.ts
  - apps/frontend/src/components/media/hooks/use-media-upload.ts
  - DIVERGENCE.md
autonomous: false
requirements:
  - R7.1
  - R7.2
  - R7.4

must_haves:
  truths:
    - "MediaLibraryModule registered in AppModule and all providers/controllers wired"
    - "Frontend media library page shows company-scoped media grid with tag filtering"
    - "Upload button uses Uppy with AwsS3Multipart plugin pointing to MinIO via S3 presigned URLs"
    - "Media items show thumbnail, name, dimensions, and tags"
  artifacts:
    - path: "extensions/media-library/src/media-library.module.ts"
      provides: "NestJS module wiring all media library providers"
      exports: ["MediaLibraryModule"]
    - path: "libraries/react-shared-libraries/src/helpers/uppy.upload.ts"
      provides: "Updated Uppy helper with 's3' case for MinIO multipart"
    - path: "apps/frontend/src/components/media/company-media-library.tsx"
      provides: "Main media library page component"
    - path: "apps/frontend/src/components/media/media-grid.tsx"
      provides: "Grid display of media items with thumbnails"
    - path: "apps/frontend/src/components/media/hooks/use-company-media.ts"
      provides: "SWR hook for fetching company media"
  key_links:
    - from: "apps/backend/src/app.module.ts"
      to: "extensions/media-library/src/media-library.module.ts"
      via: "imports array"
      pattern: "MediaLibraryModule"
    - from: "company-media-library.tsx"
      to: "/api/companies/:companySlug/media"
      via: "SWR fetch in useCompanyMedia hook"
      pattern: "useSWR.*companies.*media"
    - from: "libraries/react-shared-libraries/src/helpers/uppy.upload.ts"
      to: "MinIO S3 multipart"
      via: "AwsS3Multipart plugin, case 's3'"
      pattern: "case.*s3.*AwsS3Multipart"
---

<objective>
Wire MediaLibraryModule into the NestJS app, add Uppy S3 multipart upload support for MinIO, and build frontend media library UI with company-scoped grid, tag filtering, and Uppy-based upload. Visual checkpoint to confirm the full stack works end-to-end.

Purpose: Connects all backend pieces (storage, service, controller, processing) into a working module, wires Uppy S3 multipart for MinIO per user decision, and provides the operator-facing media library UI. This completes the media library feature for Phase 4.
Output: Working NestJS module + Uppy S3 multipart wiring + frontend media library page with upload, grid view, and tag filtering.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-media-library-processing/04-CONTEXT.md
@.planning/phases/04-media-library-processing/04-RESEARCH.md
@.planning/phases/04-media-library-processing/04-01-SUMMARY.md
@.planning/phases/04-media-library-processing/04-02-SUMMARY.md
@.planning/phases/04-media-library-processing/04-03-SUMMARY.md

@apps/backend/src/app.module.ts
@apps/frontend/src/components/media
@apps/frontend/src/app/colors.scss
@apps/frontend/src/app/global.scss
@apps/frontend/tailwind.config.js
@libraries/helpers/src/utils/custom.fetch.tsx
@libraries/react-shared-libraries/src/helpers/uppy.upload.ts
@libraries/nestjs-libraries/src/upload/r2.uploader.ts

<interfaces>
<!-- From Plans 01-03 outputs -->
From extensions/media-library/src/index.ts:
```typescript
export { MediaLibraryModule } from './media-library.module';
export { MinioStorage } from './storage/minio.storage';
export { CompanyMediaService } from './media/company-media.service';
export { CompanyMediaController } from './media/company-media.controller';
export { CompanyMediaRepository } from './media/company-media.repository';
export { MediaProcessingService } from './processing/media-processing.service';
export { MediaProcessingJob } from './processing/media-processing.job';
export { PlatformMediaValidator } from './processing/platform-media-validator';
export { PLATFORM_MEDIA_SPECS, PLATFORM_VARIANT_SPECS } from './processing/platform-specs';
export * from './types';
```

From extensions/media-library/src/storage/minio.storage.ts:
```typescript
export class MinioStorage implements IUploadProvider {
  // Also exposes multipart methods for Uppy presigned URL flow:
  createMultipartUpload(key, contentType): Promise<CreateMultipartUploadCommandOutput>;
  signPart(key, uploadId, partNumber): Promise<string>;
}
export async function uploadBufferToMinio(client, bucket, key, buffer, contentType): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaLibraryModule wiring, AppModule registration, and Uppy S3 multipart for MinIO</name>
  <files>
    extensions/media-library/src/media-library.module.ts,
    extensions/media-library/src/index.ts,
    apps/backend/src/app.module.ts,
    libraries/react-shared-libraries/src/helpers/uppy.upload.ts,
    DIVERGENCE.md
  </files>
  <action>
1. Create `media-library.module.ts`:
   - `@Module({})` with ScheduleModule.forRoot() import (for @Cron support)
   - Controllers: [CompanyMediaController]
   - Providers:
     - CompanyMediaService
     - CompanyMediaRepository
     - MediaProcessingService
     - MediaProcessingJob
     - PlatformMediaValidator
     - MinioStorage provider via useFactory: reads MINIO_* env vars, returns MinioStorage instance
   - Exports: [CompanyMediaService, MediaProcessingService, PlatformMediaValidator] (for Phase 5/6 consumers)
   - `onModuleInit()`: call MinioStorage.ensureBucket() to idempotently create the MinIO bucket on startup

2. Update `src/index.ts` to export MediaLibraryModule and all public services.

3. Add `MediaLibraryModule` to AppModule imports array in `apps/backend/src/app.module.ts`.
   - Update DIVERGENCE.md with this upstream file modification.

4. Ensure ScheduleModule is not double-imported (check if already in AppModule -- if yes, remove from MediaLibraryModule and rely on AppModule's import).

5. Add `case 's3'` to `libraries/react-shared-libraries/src/helpers/uppy.upload.ts` per user decision ("Frontend Uppy upload: use S3 multipart upload path (already exists for cloudflare) pointing to MinIO"). Follow RESEARCH.md Pattern 7:
   - Read the existing `uppy.upload.ts` file to understand the current switch/case structure and the `cloudflare` case that uses `AwsS3Multipart` plugin.
   - Add a `case 's3':` block that uses the same `AwsS3Multipart` plugin pattern as the `cloudflare` case — identical configuration with `createMultipartUpload`, `listParts`, `signPart`, `abortMultipartUpload`, `completeMultipartUpload` callbacks that delegate to `fetchUploadApiEndpoint`.
   - The backend presigned URL endpoints already exist via the existing `/media/:endpoint` pattern in `r2.uploader.ts`. If CompanyMediaController needs its own multipart endpoints (at `/companies/:companySlug/media/multipart/:endpoint`), add them to CompanyMediaController delegating to MinioStorage's multipart methods. Check whether the existing `/media/:endpoint` routes work with MinIO's S3Client or if company-scoped endpoints are needed.
   - Update DIVERGENCE.md with this upstream file modification to `uppy.upload.ts`.
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --noEmit --project extensions/media-library/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>MediaLibraryModule compiles, all providers wired (including Plan 03 artifacts: MediaProcessingService, MediaProcessingJob, PlatformMediaValidator), AppModule imports it, Uppy uppy.upload.ts has 's3' case with AwsS3Multipart, DIVERGENCE.md updated, bucket auto-creation on startup</done>
</task>

<task type="auto">
  <name>Task 2: Frontend media library UI components with Uppy upload</name>
  <files>
    apps/frontend/src/components/media/company-media-library.tsx,
    apps/frontend/src/components/media/media-grid.tsx,
    apps/frontend/src/components/media/media-upload-button.tsx,
    apps/frontend/src/components/media/hooks/use-company-media.ts,
    apps/frontend/src/components/media/hooks/use-media-upload.ts
  </files>
  <action>
1. Create SWR hooks (each in separate file per CLAUDE.md rules):
   - `use-company-media.ts`: `useCompanyMedia(companySlug, page, tag?)` -- useSWR with useFetch from `@gitroom/helpers/utils/custom.fetch`, returns { data, error, isLoading, mutate }
   - `use-media-upload.ts`: `useMediaUpload()` -- sets up Uppy instance with the S3 multipart plugin via the helper from `libraries/react-shared-libraries/src/helpers/uppy.upload.ts` (using the new 's3' case when STORAGE_PROVIDER=s3). Returns Uppy instance and an `uploadMedia(companySlug, file, tags?)` function. On upload complete, calls SWR mutate to refresh the media grid. Follow the existing Uppy usage patterns in the codebase (check how other components use Uppy with the uppy.upload.ts helper).

2. Create `media-grid.tsx`:
   - Grid layout: Tailwind CSS grid (4 cols on lg, 3 on md, 2 on sm)
   - Each card shows: thumbnail image, file name (truncated), dimensions (WxH), format badge, tag chips
   - Hover state: subtle shadow/border change
   - Empty state: "No media uploaded yet" centered message
   - Check existing component styles in `/apps/frontend/src/app/colors.scss` and other components before writing -- match existing design patterns
   - Do NOT use deprecated --color-custom* CSS variables

3. Create `media-upload-button.tsx`:
   - Button styled consistently with existing UI components
   - Uses Uppy dashboard or drag-drop for file selection (leveraging the Uppy instance from `useMediaUpload` hook), accepts image/* only
   - Shows upload progress via Uppy's built-in progress tracking
   - On complete: calls SWR mutate to refresh grid
   - Optional tag input (text field with comma-separated tags)

4. Create `company-media-library.tsx` (main page component):
   - Composes: MediaUploadButton + tag filter dropdown + MediaGrid + pagination
   - Tag filter: dropdown/pills showing available tags, selecting one passes to useCompanyMedia
   - Pagination: simple prev/next buttons using page from API response
   - Gets companySlug from URL query param `?c={slug}` (Phase 1 company switcher pattern)
   - Layout: upload button top-right, tag filter top-left, grid below, pagination bottom

5. All components are native (no npm UI libraries per CLAUDE.md). Use Tailwind 3 for styling.
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --noEmit --project apps/frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Frontend compiles, media library page renders grid with Uppy-based upload button using S3 multipart, SWR hooks in separate files, tag filtering works, no deprecated CSS vars used</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete media library: MinIO storage, Uppy S3 multipart upload, thumbnail + metadata extraction, async variant generation, platform validation, NestJS module wiring, and frontend media library UI</what-built>
  <how-to-verify>
    1. Start the dev environment: `pnpm run dev:docker` then `pnpm run dev`
    2. Navigate to the media library page for a test company
    3. Upload an image file using the Uppy-powered upload button -- verify S3 multipart upload completes (check network tab for presigned URL requests)
    4. Verify uploaded image appears in the grid with thumbnail
    5. Check that media metadata (dimensions, format) displays correctly
    6. Trigger variant generation for a media item (click "Generate Variants" for a platform)
    7. Verify the upload returns quickly (< 2s) -- variant generation is async
    8. After ~30 seconds, refresh and verify variants were created
    9. Test tag filtering: add tags to an upload, filter by tag
    10. Verify company isolation: switch companies, confirm media lists are separate
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compiles both backend and frontend
- `pnpm test --filter @social/media-library` all tests green
- MediaLibraryModule registered in AppModule
- Uppy uppy.upload.ts contains `case 's3'` with AwsS3Multipart plugin
- Frontend media library page renders with grid, Uppy upload, filtering
- Upload flow works end-to-end: Uppy S3 multipart -> MinIO -> thumbnail -> DB -> grid
- Variant generation is async (response returns before processing completes)
</verification>

<success_criteria>
- Full stack working: Uppy S3 multipart upload -> store -> display -> process -> validate
- Uppy uses S3 multipart upload path pointing to MinIO (per user locked decision)
- Company-scoped media isolation verified
- Frontend matches existing design patterns
- All SWR hooks in separate files per CLAUDE.md rules
- NestJS module wired correctly with all providers (including Plan 03 processing artifacts)
- Async processing confirmed (upload response < 2s)
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-library-processing/04-04-SUMMARY.md`
</output>
