---
phase: 04-media-library-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/media-library/package.json
  - extensions/media-library/tsconfig.json
  - extensions/media-library/tsconfig.spec.json
  - extensions/media-library/jest.config.ts
  - extensions/media-library/src/index.ts
  - extensions/media-library/src/storage/minio.storage.ts
  - extensions/media-library/src/processing/platform-specs.ts
  - extensions/media-library/src/types.ts
  - extensions/media-library/src/__tests__/minio.storage.spec.ts
  - tsconfig.base.json
  - libraries/nestjs-libraries/src/upload/upload.factory.ts
  - libraries/nestjs-libraries/src/database/prisma/schema.prisma
  - prisma/migrations/20260310100000_media_library/migration.sql
  - DIVERGENCE.md
  - .env.example
autonomous: true
requirements:
  - R7.1
  - R7.2
  - R7.5

must_haves:
  truths:
    - "MinioStorage implements IUploadProvider and can upload/delete objects to MinIO with forcePathStyle"
    - "Media model has companyId, width, height, format, tags fields"
    - "MediaVariant and MediaProcessingJob Prisma models exist"
    - "UploadFactory returns MinioStorage when STORAGE_PROVIDER=s3"
  artifacts:
    - path: "extensions/media-library/package.json"
      provides: "@social/media-library extension package"
    - path: "extensions/media-library/src/storage/minio.storage.ts"
      provides: "S3-compatible MinIO storage provider"
      exports: ["MinioStorage"]
    - path: "extensions/media-library/src/types.ts"
      provides: "Shared type contracts for media library"
    - path: "extensions/media-library/src/processing/platform-specs.ts"
      provides: "Platform dimension specs and validation types"
    - path: "libraries/nestjs-libraries/src/upload/upload.factory.ts"
      provides: "Updated factory with s3 case"
  key_links:
    - from: "libraries/nestjs-libraries/src/upload/upload.factory.ts"
      to: "extensions/media-library/src/storage/minio.storage.ts"
      via: "import MinioStorage, case 's3'"
      pattern: "case.*s3.*MinioStorage"
    - from: "extensions/media-library/src/storage/minio.storage.ts"
      to: "@aws-sdk/client-s3"
      via: "S3Client with forcePathStyle: true"
      pattern: "forcePathStyle.*true"
---

<objective>
Scaffold the media-library extension package, implement MinioStorage provider, define all Prisma schema extensions (Media fields + MediaVariant + MediaProcessingJob), wire UploadFactory's s3 case, and establish type contracts for downstream plans.

Purpose: Foundation layer -- all subsequent media plans depend on the storage provider, schema, and type contracts created here.
Output: Working extension package with MinIO storage, Prisma migration, type contracts, and test infrastructure.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-media-library-processing/04-CONTEXT.md
@.planning/phases/04-media-library-processing/04-RESEARCH.md

@libraries/nestjs-libraries/src/upload/upload.factory.ts
@libraries/nestjs-libraries/src/upload/upload.interface.ts
@libraries/nestjs-libraries/src/upload/cloudflare.storage.ts
@libraries/nestjs-libraries/src/database/prisma/schema.prisma
@extensions/ai-service/package.json
@extensions/ai-service/tsconfig.json
@extensions/ai-service/jest.config.ts
@tsconfig.base.json
@DIVERGENCE.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extension scaffold, type contracts, platform specs, and Prisma schema</name>
  <files>
    extensions/media-library/package.json,
    extensions/media-library/tsconfig.json,
    extensions/media-library/tsconfig.spec.json,
    extensions/media-library/jest.config.ts,
    extensions/media-library/src/index.ts,
    extensions/media-library/src/types.ts,
    extensions/media-library/src/processing/platform-specs.ts,
    libraries/nestjs-libraries/src/database/prisma/schema.prisma,
    tsconfig.base.json,
    .env.example
  </files>
  <action>
1. Create `extensions/media-library/` package mirroring `extensions/ai-service/` structure:
   - `package.json`: name `@social/media-library`, main `src/index.ts`
   - `tsconfig.json`: extends `../../tsconfig.base.json`
   - `tsconfig.spec.json`: for ts-jest (mirror ai-service pattern)
   - `jest.config.ts`: ts-jest config mirroring ai-service/jest.config.ts

2. Create `src/types.ts` with shared type contracts:
   - `MediaUploadResult`: { id, path, thumbnailPath, width, height, format, fileSize }
   - `VariantSpec`: { platform, width, height }
   - `VariantResult`: { buffer, width, height, format, fileSize }
   - `PlatformValidationResult`: { platform, passed, reasons: string[] }
   - `MediaProcessingJobStatus`: 'pending' | 'processing' | 'completed' | 'failed'

3. Create `src/processing/platform-specs.ts`:
   - Export `PLATFORM_MEDIA_SPECS` constant with specs per CONTEXT.md:
     Instagram: 1080x1080 (square), 1080x1350 (portrait), 1080x566 (landscape)
     Facebook: 1200x630, LinkedIn: 1200x627, X: 1200x675
   - Export `PLATFORM_VARIANT_SPECS: Record<string, VariantSpec[]>` mapping platform name to array of VariantSpec
   - Max file sizes: Instagram 8MB, Facebook 4MB, LinkedIn 5MB, X 5MB
   - Formats: ['jpeg', 'jpg', 'png'] for all platforms

4. Extend Prisma schema (DIVERGENCE.md entry required):
   - Add to existing `Media` model: `companyId String?`, `width Int?`, `height Int?`, `format String?`, `tags String[] @default([])`
   - Add `company Company? @relation(fields: [companyId], references: [id])` relation
   - Add `variants MediaVariant[]` relation
   - Add indexes: `@@index([companyId])`, `@@index([companyId, createdAt])`
   - Add new `MediaVariant` model: id (uuid), mediaId, platform, width, height, path, fileSize, createdAt; `@@unique([mediaId, platform, width, height])`, `@@index([mediaId])`
   - Add new `MediaProcessingJob` model: id (uuid), mediaId, platforms (String[]), status (String default "pending"), error (String?), createdAt, updatedAt; `@@index([status, createdAt])`
   - Create manual migration SQL file at `prisma/migrations/20260310100000_media_library/migration.sql`

5. Add `@social/media-library` path alias to `tsconfig.base.json` paths (follow pattern of existing `@social/*` entries)

6. Add MinIO env vars to `.env.example`:
   ```
   # MinIO / S3 Storage (Phase 4)
   STORAGE_PROVIDER=s3
   MINIO_ENDPOINT=http://localhost:9000
   MINIO_ACCESS_KEY=minioadmin
   MINIO_SECRET_KEY=minioadmin
   MINIO_BUCKET=postiz-media
   MINIO_PUBLIC_URL=http://localhost:9000/postiz-media
   ```

7. Create `src/index.ts` exporting all types and platform-specs (storage export added in Task 2).
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --noEmit --project extensions/media-library/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Extension package compiles, Prisma schema valid with new models, tsconfig paths resolve @social/media-library, .env.example has MinIO vars, platform specs exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MinioStorage provider and UploadFactory wiring</name>
  <files>
    extensions/media-library/src/storage/minio.storage.ts,
    extensions/media-library/src/__tests__/minio.storage.spec.ts,
    libraries/nestjs-libraries/src/upload/upload.factory.ts,
    DIVERGENCE.md,
    extensions/media-library/src/index.ts
  </files>
  <behavior>
    - Test: MinioStorage constructor creates S3Client with forcePathStyle: true
    - Test: uploadFile sends PutObjectCommand with correct Bucket, Key, Body, ContentType
    - Test: uploadFile returns object with path = publicUrl/key
    - Test: uploadSimple fetches URL and uploads buffer to S3
    - Test: removeFile sends DeleteObjectCommand with correct key extracted from path
  </behavior>
  <action>
1. Create `src/storage/minio.storage.ts` implementing `IUploadProvider` interface from `@gitroom/nestjs-libraries/upload/upload.interface`:
   - Constructor takes: endpoint, accessKeyId, secretAccessKey, bucketName, publicUrl
   - Creates S3Client with `forcePathStyle: true` (CRITICAL for MinIO), region 'us-east-1'
   - `uploadFile(file)`: generates key via makeId + mime extension, sends PutObjectCommand, returns standard upload result with path = `${publicUrl}/${key}`
   - `uploadSimple(path)`: fetches URL, uploads buffer via PutObjectCommand, returns public URL string
   - `removeFile(filePath)`: extracts key by removing publicUrl prefix, sends DeleteObjectCommand
   - Add `onModuleInit`-style static method `ensureBucket(client, bucket)` that calls HeadBucketCommand and CreateBucketCommand if 404 (idempotent bucket creation)
   - Export `uploadBufferToMinio(client, bucket, key, buffer, contentType)` utility for direct buffer uploads (used by thumbnail and variant upload in Plan 02/03)

2. Write tests in `src/__tests__/minio.storage.spec.ts`:
   - Mock `@aws-sdk/client-s3` (jest.mock) -- mock S3Client.send to capture commands
   - Test all 5 behaviors listed above
   - Verify forcePathStyle in client config

3. Add `case 's3'` to `UploadFactory.createStorage()` in `libraries/nestjs-libraries/src/upload/upload.factory.ts`:
   - Import MinioStorage from `@social/media-library`
   - Instantiate with MINIO_ENDPOINT, MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_BUCKET, MINIO_PUBLIC_URL env vars
   - Update DIVERGENCE.md with this upstream file modification

4. Update `src/index.ts` to export MinioStorage and uploadBufferToMinio.
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media/extensions/media-library" && pnpm test -- --testPathPattern="minio.storage.spec" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>MinioStorage passes all 5 tests, UploadFactory has s3 case, DIVERGENCE.md updated, exports working</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project extensions/media-library/tsconfig.json` compiles clean
- `pnpm test --testPathPattern="minio.storage.spec"` all tests green
- `npx prisma validate` passes with new schema fields
- UploadFactory.ts contains `case 's3'` with MinioStorage import
</verification>

<success_criteria>
- Extension package scaffold complete with jest, tsconfig, types
- MinioStorage implements IUploadProvider with forcePathStyle: true
- Prisma schema has Media extensions (companyId, width, height, format, tags), MediaVariant, MediaProcessingJob
- UploadFactory wired with s3 case
- All tests green
- DIVERGENCE.md updated
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-library-processing/04-01-SUMMARY.md`
</output>
