---
phase: 01-fork-and-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - apps/frontend/src/components/company-switcher/company-switcher.tsx
  - apps/frontend/src/components/company-switcher/company-context.tsx
  - apps/frontend/src/components/company-switcher/use-companies.ts
  - apps/frontend/src/app/(app)/(site)/layout.tsx
  - DIVERGENCE.md
autonomous: false
requirements: [R2.7]

must_haves:
  truths:
    - "Company switcher dropdown is visible in the top navigation header on all company-scoped pages"
    - "Selecting a different company from the dropdown navigates to that company's view while staying on the same page type"
    - "URL updates with the selected company slug (e.g., /verde-kitchen/launches)"
    - "All page content is scoped to the currently selected company"
    - "Default company is auto-selected on first visit (redirect from / to /{firstCompanySlug}/)"
  artifacts:
    - path: "apps/frontend/src/components/company-switcher/company-switcher.tsx"
      provides: "Dropdown component for switching between companies"
      contains: "CompanySwitcher"
    - path: "apps/frontend/src/components/company-switcher/company-context.tsx"
      provides: "React context provider for current company"
      contains: "CompanyContext"
    - path: "apps/frontend/src/components/company-switcher/use-companies.ts"
      provides: "SWR hook for fetching companies list"
      contains: "useCompanies"
  key_links:
    - from: "apps/frontend/src/components/company-switcher/company-switcher.tsx"
      to: "/api/companies"
      via: "SWR data fetching"
      pattern: "useCompanies|/api/companies"
    - from: "apps/frontend/src/components/company-switcher/company-switcher.tsx"
      to: "apps/frontend/src/app/(app)/(site)/layout.tsx"
      via: "Rendered in layout header"
      pattern: "CompanySwitcher"
    - from: "apps/frontend/src/components/company-switcher/company-context.tsx"
      to: "apps/frontend/src/components/company-switcher/company-switcher.tsx"
      via: "Context provides current company to all children"
      pattern: "CompanyContext\\.Provider"
---

<objective>
Build the company switcher UI component and integrate it into the Postiz layout, enabling the operator to switch between companies while staying on the same page.

Purpose: Give the operator a visible, always-accessible way to switch company context, with URL-based scoping for bookmarkability and clear context. This is the primary UX mechanism for multi-company navigation.

Output: Working company switcher in header, URL-scoped routing, company context available to all page components.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-CONTEXT.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@.planning/phases/01-fork-and-foundation/01-03-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 03 that this plan consumes -->

GET /api/companies — returns Company[] with nested brands, brandVoice, socialAccounts
GET /api/companies/:slug — returns single Company with full include tree

Company shape:
```typescript
{
  id: string;
  name: string;
  slug: string;
  timezone: string;
  defaultLanguage: string;
  industry: string | null;
  website: string | null;
  logo: string | null;
  notes: string | null;
  brands: Brand[];
}
```

Brand shape:
```typescript
{
  id: string;
  companyId: string;
  name: string;
  slug: string;
  logo: string | null;
  description: string | null;
  brandVoice: BrandVoice | null;
  socialAccounts: SocialAccount[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create company switcher component and integrate into layout</name>
  <files>
    apps/frontend/src/components/company-switcher/company-switcher.tsx,
    apps/frontend/src/components/company-switcher/company-context.tsx,
    apps/frontend/src/components/company-switcher/use-companies.ts,
    apps/frontend/src/components/company-switcher/index.ts,
    apps/frontend/src/app/(app)/(site)/layout.tsx,
    DIVERGENCE.md
  </files>
  <action>
    1. **Understand the existing Postiz frontend patterns first:**
       - Read `apps/frontend/src/app/(app)/(site)/layout.tsx` to understand current layout structure
       - Check how Postiz handles data fetching (likely SWR hooks based on research)
       - Check existing UI component patterns (Tailwind CSS classes, component structure)
       - Check existing navigation/header patterns to find the right insertion point

    2. Create `apps/frontend/src/components/company-switcher/use-companies.ts`:
       - SWR hook `useCompanies()` that fetches from `/api/companies`
       - Returns `{ companies, isLoading, error }`
       - Use Postiz's existing fetch/API patterns if present (check for existing SWR wrappers)

    3. Create `apps/frontend/src/components/company-switcher/company-context.tsx`:
       - React context: `CompanyContext` with value `{ company: Company | null, companySlug: string | null }`
       - `CompanyProvider` component that:
         a. Reads companySlug from URL (using Next.js `useParams()` or equivalent)
         b. Fetches company data for the slug from the companies list
         c. Provides to children via context
       - `useCompany()` hook that reads from context (throws if used outside provider)

    4. Create `apps/frontend/src/components/company-switcher/company-switcher.tsx`:
       - Dropdown component styled like Slack workspace switcher:
         a. Shows current company name (or logo if available) in a clickable trigger
         b. Dropdown shows all companies, each with name and industry
         c. Clicking a company navigates to `/{companySlug}/{currentPage}` preserving the current page
       - Use Next.js `useRouter()` and `usePathname()` for navigation
       - Use Tailwind CSS for styling (match Postiz design patterns)
       - Loading state: show skeleton while companies load
       - Handle edge case: if only 1 company, still show switcher but indicate it's the only one

    5. Create `apps/frontend/src/components/company-switcher/index.ts`:
       - Barrel export: CompanySwitcher, CompanyProvider, useCompany, useCompanies

    6. Integrate into the Postiz layout:
       - Modify `apps/frontend/src/app/(app)/(site)/layout.tsx`:
         a. Import CompanySwitcher and CompanyProvider
         b. Add CompanySwitcher in the header/nav area (find the existing nav element)
         c. Wrap page content with CompanyProvider
       - This is an upstream file modification -- log in DIVERGENCE.md

    7. Handle routing:
       - If user visits `/` (no company slug), redirect to `/{firstCompanySlug}/` (first company alphabetically)
       - If companySlug in URL doesn't match any company, show a "Company not found" message
       - When switching companies, replace the companySlug segment in the current URL path

    IMPORTANT: Per user decision, the company switcher is a header dropdown (like Slack workspace switcher), always visible in top navigation.
    IMPORTANT: Per user decision, URL must include company slug for bookmarkability (e.g., /verde-kitchen/launches).
    IMPORTANT: Per user decision, switching companies stays on the same page type.
    IMPORTANT: Postiz frontend uses SWR hooks for data fetching (not Server Components async/await). Follow this pattern.
    IMPORTANT: Minimize upstream layout modifications. Log everything in DIVERGENCE.md.

    NOTE: The exact routing approach needs to be adapted to Postiz's actual route structure discovered during implementation. The research identified a potential conflict (Pitfall #5) -- if moving pages under [companySlug] requires moving upstream files, prefer a layout-level approach that wraps existing pages instead. Use a redirect from `/` to `/{slug}/` and a company-aware layout that reads the slug, rather than restructuring the entire route tree.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -30 && echo "Frontend TypeScript compiles"</automated>
  </verify>
  <done>
    - CompanySwitcher dropdown renders in the header with all seeded companies
    - Selecting a company updates the URL with the company slug
    - CompanyContext provides current company data to all child components
    - Visiting / redirects to /{firstCompanySlug}/
    - DIVERGENCE.md updated for layout.tsx modification
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify company switcher UX and data isolation</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the complete Phase 1 deliverable end-to-end. This is a visual and functional check that Claude cannot automate -- confirming the company switcher looks correct, feels right in the UI, and the full multi-company flow works as expected.

    What was built: Complete Phase 1 deliverable -- fork, Docker environment, extension architecture, multi-company data model, CRUD API, seed data, isolation tests, and company switcher UI.

    Steps for verification:
    1. Start the dev environment: `docker compose -f docker/docker-compose.dev.yaml up -d`
    2. Run migrations and seed: `pnpm run prisma:migrate && pnpm run prisma:seed`
    3. Start the app (check Postiz docs for start command, likely `pnpm dev` or `npx nx serve backend` + `npx nx serve frontend`)
    4. Open the browser -- should redirect to /{first-company-slug}/
    5. Verify the company switcher is visible in the top nav header
    6. Click the switcher -- should show all 3 companies: Verde Kitchen, Nexus AI, Aura Fashion
    7. Select "Nexus AI" -- URL should change to /nexus-ai/... and stay on the same page
    8. Select "Aura Fashion" -- URL should change to /aura-fashion/...
    9. Verify the page context updates (header shows selected company name)
    10. Run isolation tests: `npx nx test multi-company` -- all tests should pass
    11. Check DIVERGENCE.md has entries for all upstream modifications

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>npx nx test multi-company 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Company switcher dropdown renders correctly in header with all 3 seeded companies
    - Switching companies updates URL and stays on same page
    - Isolation tests pass
    - User types "approved" to confirm
  </done>
</task>

</tasks>

<verification>
- Company switcher is visible and functional in the header
- URL updates with company slug on selection
- All 3 seeded companies appear in the dropdown
- Switching companies preserves the current page type
- Frontend TypeScript compiles without errors
- DIVERGENCE.md documents all upstream file modifications
</verification>

<success_criteria>
- Company switcher dropdown works like Slack workspace switcher in the header
- Selecting a company navigates to /{companySlug}/ while staying on the same page
- URL-based company scoping provides bookmarkable, clear context
- React CompanyContext makes current company available to all child components
- Human verification confirms the full Phase 1 deliverable works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-05-SUMMARY.md`
</output>
