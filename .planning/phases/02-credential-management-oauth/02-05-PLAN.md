---
phase: 02-credential-management-oauth
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/frontend/src/components/credential-health/BrandConnectPanel.tsx
  - apps/frontend/src/components/credential-health/useBrandConnections.ts
  - apps/frontend/src/components/credential-health/index.ts
  - .env.example
autonomous: false

requirements: [R3.1, R3.6]

user_setup:
  - service: meta-business
    why: "OAuth for Instagram + Facebook"
    env_vars:
      - name: META_APP_ID
        source: "Meta Developer Dashboard -> App Settings -> Basic"
      - name: META_APP_SECRET
        source: "Meta Developer Dashboard -> App Settings -> Basic"
    dashboard_config:
      - task: "Create Meta App with Instagram Basic Display + Facebook Login permissions"
        location: "developers.facebook.com -> My Apps"
      - task: "Add OAuth redirect URI: http://localhost:5000/api/integrations/social/facebook/callback"
        location: "Meta App -> Facebook Login -> Settings -> Valid OAuth Redirect URIs"
  - service: linkedin
    why: "OAuth for LinkedIn"
    env_vars:
      - name: LINKEDIN_CLIENT_ID
        source: "LinkedIn Developer Portal -> App -> Auth tab"
      - name: LINKEDIN_CLIENT_SECRET
        source: "LinkedIn Developer Portal -> App -> Auth tab"
    dashboard_config:
      - task: "Create LinkedIn App with Marketing API product"
        location: "developer.linkedin.com -> My Apps"
      - task: "Add redirect URI: http://localhost:5000/api/integrations/social/linkedin/callback"
        location: "LinkedIn App -> Auth -> Authorized redirect URLs"
  - service: x-twitter
    why: "OAuth for X (Twitter)"
    env_vars:
      - name: X_API_KEY
        source: "X Developer Portal -> Project -> Keys and Tokens"
      - name: X_API_SECRET
        source: "X Developer Portal -> Project -> Keys and Tokens"
    dashboard_config:
      - task: "Create X App with OAuth 1.0a enabled"
        location: "developer.x.com -> Projects & Apps"
      - task: "Add callback URL: http://localhost:5000/api/integrations/social/x/callback"
        location: "X App -> Authentication Settings"
  - service: encryption
    why: "Token encryption at rest (NF1.1)"
    env_vars:
      - name: ENCRYPTION_KEY
        source: "Generate with: openssl rand -hex 32"

must_haves:
  truths:
    - "Operator can initiate OAuth connect from a brand settings context"
    - "After OAuth callback, the integration is linked to the correct brand's SocialAccount"
    - "Brand connect panel shows which platforms are connected and their health state"
    - "Operator can see connect buttons for unconnected platforms (Instagram, Facebook, LinkedIn, X)"
    - "ENCRYPTION_KEY documented in .env.example"
  artifacts:
    - path: "apps/frontend/src/components/credential-health/BrandConnectPanel.tsx"
      provides: "UI panel showing connected platforms per brand with connect buttons"
    - path: "apps/frontend/src/components/credential-health/useBrandConnections.ts"
      provides: "SWR hook for brand-specific connection data"
    - path: ".env.example"
      provides: "Updated with ENCRYPTION_KEY and OAuth env var documentation"
  key_links:
    - from: "apps/frontend/src/components/credential-health/BrandConnectPanel.tsx"
      to: "/api/credentials/oauth/start"
      via: "fetch POST to initiate OAuth"
      pattern: "credentials/oauth/start"
    - from: "apps/frontend/src/components/credential-health/BrandConnectPanel.tsx"
      to: "useTokenHealth"
      via: "shows health state per connected platform"
      pattern: "useTokenHealth"
---

<objective>
Create the brand-scoped connect UI panel and update .env.example with all required credentials, completing the end-to-end OAuth connect experience.

Purpose: R3.1 requires the operator to connect accounts to brands. This plan creates the frontend connect panel that initiates OAuth flows via the Plan 03 controller, displays connection status, and documents all required environment variables.
Output: BrandConnectPanel component, updated .env.example, end-to-end connect flow ready for manual testing.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-credential-management-oauth/02-RESEARCH.md
@.planning/phases/02-credential-management-oauth/02-03-SUMMARY.md
@.planning/phases/02-credential-management-oauth/02-04-SUMMARY.md

Frontend patterns:
- Use useFetch from @gitroom/helpers/utils/custom.fetch
- SWR hooks: each in separate function
- Tailwind 3: check colors.scss, global.scss, tailwind.config.js
- No npm UI component libraries
- Check existing UI components in apps/frontend/src/components/ui

<interfaces>
<!-- From Plan 03: OAuthBrandController endpoints -->
POST /api/credentials/oauth/start
  Body: { provider: string, brandId: string, companyId: string }
  Response: { url: string, state: string }

POST /api/credentials/oauth/callback
  Body: { provider: string, state: string, code: string }
  Response: { success: boolean, integrationId: string }

<!-- From Plan 04: useTokenHealth hook -->
function useTokenHealth(): {
  data: TokenHealthResponse | undefined;
  error: any;
  isLoading: boolean;
}

<!-- Phase 1 Brand model (from seed data): -->
Brand { id, companyId, name, slug, ... }

<!-- SocialAccount linking: -->
SocialAccount { brandId, platform, integrationId (nullable) }

<!-- Supported platforms for MVP: -->
const MVP_PLATFORMS = ['instagram', 'facebook', 'linkedin', 'x'];
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand connect panel component and SWR hook</name>
  <files>
    apps/frontend/src/components/credential-health/BrandConnectPanel.tsx,
    apps/frontend/src/components/credential-health/useBrandConnections.ts,
    apps/frontend/src/components/credential-health/index.ts
  </files>
  <action>
    1. Create useBrandConnections.ts:
       - Import useFetch and useSWR
       - Export function useBrandConnections(brandId: string) that fetches brand's social accounts
       - Endpoint: GET /api/companies/{companySlug}/brands/{brandSlug}/connections (or derive from useTokenHealth filtering by brandId)
       - Returns: { connections, isLoading, error, mutate }
       - Each connection: { platform, connected: boolean, integrationId?, health?, displayName? }

    2. Create BrandConnectPanel.tsx:
       - Props: { brandId: string, brandSlug: string, companyId: string, companySlug: string }
       - Uses useBrandConnections(brandId) and useTokenHealth()
       - Renders a grid (2 cols on md, 1 on mobile) of platform cards
       - Each card for MVP platforms (Instagram, Facebook, LinkedIn, X):
         a. Platform icon/name at top
         b. If connected: show display name, health indicator dot (green/amber/red), "Disconnect" link
         c. If not connected: show "Connect" button
       - "Connect" button onClick:
         a. POST /api/credentials/oauth/start with { provider, brandId, companyId }
         b. Redirect browser to returned OAuth URL (window.location.href = url)
       - Tailwind styling: rounded cards with border, hover state, consistent with existing UI
       - Do NOT use deprecated --color-custom* variables

    3. Create index.ts barrel:
       - Export TokenHealthBanner, TokenHealthList, BrandConnectPanel, useTokenHealth, useBrandConnections
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --project apps/frontend/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>BrandConnectPanel renders 4 platform cards with connect/disconnect state, initiates OAuth via /api/credentials/oauth/start</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete OAuth credential management system:
    - AES-256-GCM token encryption (Plan 01)
    - Proactive token refresh cron job at 75% lifetime (Plan 02)
    - Token health state tracking with 4 states (Plan 02)
    - OAuth brand-context controller linking integrations to brands (Plan 03)
    - Token health API + dashboard banner + health list (Plan 04)
    - Brand connect panel with platform cards (Plan 05)
    - All environment variables documented in .env.example
  </what-built>
  <how-to-verify>
    Prerequisites: Docker running (pnpm run dev:docker), migrations applied (pnpm run prisma:migrate), env vars set.

    1. Start the app: pnpm run dev
    2. Navigate to a brand settings page (e.g., /companies/verde-kitchen/brands/verde-main)
    3. Verify the BrandConnectPanel shows 4 platform cards (Instagram, Facebook, LinkedIn, X) all showing "Connect"
    4. Check /api/credentials/health returns empty integrations array (none connected yet)
    5. If you have Meta/LinkedIn/X API credentials configured:
       a. Click "Connect" on one platform
       b. Complete the OAuth flow
       c. Verify the platform card now shows connected with health indicator
       d. Check /api/credentials/health returns the new integration
    6. Verify TokenHealthBanner does NOT show when all connections are healthy (or none exist)
    7. Check the token health list page shows all connections with proper styling
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles
- BrandConnectPanel renders correctly with connect buttons
- Full OAuth flow: start -> redirect -> callback -> SocialAccount linked -> health visible
- .env.example documents all required variables
</verification>

<success_criteria>
- Operator can see 4 platform connect cards on brand settings
- Connect button initiates OAuth flow via /api/credentials/oauth/start
- After OAuth, integration is linked to brand and health is visible
- .env.example has ENCRYPTION_KEY and all OAuth provider env vars documented
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-management-oauth/02-05-SUMMARY.md`
</output>
