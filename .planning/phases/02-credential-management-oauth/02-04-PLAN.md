---
phase: 02-credential-management-oauth
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - extensions/credential-management/src/health/token.health.controller.ts
  - extensions/credential-management/src/credential-management.module.ts
  - apps/frontend/src/components/credential-health/TokenHealthBanner.tsx
  - apps/frontend/src/components/credential-health/TokenHealthList.tsx
  - apps/frontend/src/components/credential-health/useTokenHealth.ts
autonomous: true
requirements: [R3.4, R3.5]

must_haves:
  truths:
    - "GET /api/credentials/health returns list of integrations with health state, provider, brand info"
    - "Token health banner appears on dashboard when any integration is in warning/expired/refresh_needed state"
    - "Token health list shows all connected accounts with their health state"
    - "Healthy tokens show green indicator, warning shows amber, expired/refresh_needed shows red"
  artifacts:
    - path: "extensions/credential-management/src/health/token.health.controller.ts"
      provides: "GET /api/credentials/health endpoint"
      exports: ["TokenHealthController"]
    - path: "apps/frontend/src/components/credential-health/TokenHealthBanner.tsx"
      provides: "Dashboard banner for unhealthy tokens"
    - path: "apps/frontend/src/components/credential-health/TokenHealthList.tsx"
      provides: "Full list of connected accounts with health indicators"
    - path: "apps/frontend/src/components/credential-health/useTokenHealth.ts"
      provides: "SWR hook for fetching token health data"
  key_links:
    - from: "apps/frontend/src/components/credential-health/useTokenHealth.ts"
      to: "/api/credentials/health"
      via: "useSWR with useFetch"
      pattern: "useSWR.*credentials/health"
    - from: "extensions/credential-management/src/health/token.health.controller.ts"
      to: "TokenHealthService"
      via: "getTokenHealth() called per integration"
      pattern: "tokenHealthService\\.getTokenHealth"
---

<objective>
Create the token health API endpoint and frontend components that surface token health to the operator.

Purpose: R3.4 requires a token health dashboard showing last refreshed, expiry, and failure counts. R3.5 requires alerts when refresh fails. This plan builds the API endpoint and React components.
Output: Health API endpoint, dashboard banner component, health list component.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-credential-management-oauth/02-RESEARCH.md
@.planning/phases/02-credential-management-oauth/02-01-SUMMARY.md
@.planning/phases/02-credential-management-oauth/02-02-SUMMARY.md

Frontend patterns:
- Use useFetch hook from /libraries/helpers/src/utils/custom.fetch.tsx
- Use useSWR for data fetching (each hook in separate function per CLAUDE.md)
- Tailwind 3 for styling (check tailwind.config.js and colors.scss)
- Check apps/frontend/src/components/ui for existing UI patterns
- Never install frontend components from npmjs -- write native components

<interfaces>
<!-- From Plan 02: TokenHealthService -->
export type TokenHealthState = 'healthy' | 'warning' | 'expired' | 'refresh_needed';
export class TokenHealthService {
  getTokenHealth(integration: { tokenExpiration: Date | null; refreshNeeded: boolean; consecutiveFailures: number; lastRefreshedAt: Date | null }): TokenHealthState;
}

<!-- Health endpoint response shape (from RESEARCH.md): -->
interface TokenHealthResponse {
  integrations: {
    id: string;
    provider: string;
    name: string;
    picture: string | null;
    brandId: string | null;
    brandName: string | null;
    health: TokenHealthState;
    lastRefreshedAt: string | null;
    expiresAt: string | null;
    consecutiveFailures: number;
    daysUntilExpiry: number | null;
  }[];
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token health API endpoint</name>
  <files>
    extensions/credential-management/src/health/token.health.controller.ts,
    extensions/credential-management/src/credential-management.module.ts
  </files>
  <action>
    1. Create src/health/token.health.controller.ts:
       @Controller('credentials') TokenHealthController

       GET /api/credentials/health
       - Query all non-deleted integrations with their related organization (for companyId filtering)
       - Left join to SocialAccount to get brandId and brand name
       - For each integration, compute health state via TokenHealthService.getTokenHealth()
       - Compute daysUntilExpiry from tokenExpiration
       - Return TokenHealthResponse shape (see interfaces above)
       - If x-company-slug header present, filter by company's organizations

    2. Update credential-management.module.ts: add TokenHealthController to controllers array.
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --project extensions/credential-management/tsconfig.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GET /api/credentials/health endpoint returns integration health data with brand context</done>
</task>

<task type="auto">
  <name>Task 2: Frontend token health banner and list components</name>
  <files>
    apps/frontend/src/components/credential-health/useTokenHealth.ts,
    apps/frontend/src/components/credential-health/TokenHealthBanner.tsx,
    apps/frontend/src/components/credential-health/TokenHealthList.tsx
  </files>
  <action>
    1. Create useTokenHealth.ts:
       - Import useFetch from @gitroom/helpers/utils/custom.fetch
       - Import useSWR from 'swr'
       - Export function useTokenHealth() that calls useSWR('/api/credentials/health', fetcher)
       - Return { data, error, isLoading } -- follow project SWR hook pattern (each hook is its own function)

    2. Create TokenHealthBanner.tsx:
       - Uses useTokenHealth() hook
       - If any integration has health !== 'healthy', show a warning banner
       - Banner text: "{N} connection(s) need attention" with link to settings
       - Color: amber/yellow background for warnings, red for expired/refresh_needed
       - Tailwind classes only (check existing colors.scss and tailwind.config.js)
       - If all healthy or loading, render null
       - Export as default

    3. Create TokenHealthList.tsx:
       - Uses useTokenHealth() hook
       - Renders a list/table of all connected accounts
       - Each row: provider icon/name, account name, brand name, health indicator dot (green/amber/red), last refreshed date, expiry date, failure count
       - Health dot colors: healthy=green-500, warning=amber-500, expired/refresh_needed=red-500
       - Tailwind styling consistent with existing UI components
       - Empty state: "No connected accounts" message
       - Export as default

    IMPORTANT: Check apps/frontend/src/app/colors.scss and tailwind.config.js BEFORE writing components. Use existing color variables and utility classes. Do NOT use deprecated --color-custom* variables.
  </action>
  <verify>
    <automated>cd "C:/Users/nikos/Desktop/work/coding/social media" && npx tsc --project apps/frontend/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TokenHealthBanner shows when unhealthy tokens exist; TokenHealthList shows all connections with health state indicators; both use SWR hook pattern</done>
</task>

</tasks>

<verification>
- TypeScript compiles for both backend and frontend
- Health endpoint returns correct response shape
- Frontend components follow project conventions (SWR hooks, Tailwind, no npm UI libraries)
</verification>

<success_criteria>
- GET /api/credentials/health returns all integrations with computed health state and brand context
- TokenHealthBanner renders warning when any unhealthy connections exist
- TokenHealthList renders full connection inventory with color-coded health indicators
- All components use project SWR and styling patterns
</success_criteria>

<output>
After completion, create `.planning/phases/02-credential-management-oauth/02-04-SUMMARY.md`
</output>
