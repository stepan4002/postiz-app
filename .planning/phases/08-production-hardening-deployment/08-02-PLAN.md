---
phase: 08-production-hardening-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/security/package.json
  - extensions/security/tsconfig.json
  - extensions/security/src/ssrf.guard.ts
  - extensions/security/src/__tests__/ssrf.guard.spec.ts
  - extensions/security/src/index.ts
autonomous: true
requirements: [NF1.3, NF1.4, NF1.5, NF1.6]

must_haves:
  truths:
    - "assertSafeUrl blocks private IP ranges (10.x, 172.16-31.x, 192.168.x, 127.x)"
    - "assertSafeUrl blocks non-HTTPS URLs"
    - "assertSafeUrl blocks internal hostnames (localhost, metadata.google.internal)"
    - "assertSafeUrl allows valid public HTTPS URLs"
    - "AI prompt injection safeguard is validated (user input only in user role)"
    - ".env.example documents all required env vars including DISABLE_REGISTRATION=true"
  artifacts:
    - path: "extensions/security/src/ssrf.guard.ts"
      provides: "SSRF URL validation utility"
      exports: ["assertSafeUrl"]
    - path: "extensions/security/src/__tests__/ssrf.guard.spec.ts"
      provides: "SSRF guard unit tests"
      min_lines: 30
  key_links:
    - from: "extensions/security/src/ssrf.guard.ts"
      to: "any controller accepting user URLs"
      via: "import { assertSafeUrl }"
      pattern: "assertSafeUrl"
---

<objective>
Create SSRF protection utility, validate AI prompt injection safeguards already in place, and audit .env.example for completeness including DISABLE_REGISTRATION=true for single-user auth (NF1.6).

Purpose: Security hardening before production — prevent outbound SSRF, confirm prompt injection defenses, ensure secrets are env-var driven.
Output: @social/security extension package with assertSafeUrl, updated .env.example.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-production-hardening-deployment/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSRF protection utility with comprehensive test coverage</name>
  <files>extensions/security/package.json, extensions/security/tsconfig.json, extensions/security/src/ssrf.guard.ts, extensions/security/src/__tests__/ssrf.guard.spec.ts, extensions/security/src/index.ts</files>
  <behavior>
    - assertSafeUrl("http://example.com") throws "Only HTTPS URLs allowed"
    - assertSafeUrl("https://192.168.1.1/path") throws "Private IP ranges are not allowed"
    - assertSafeUrl("https://10.0.0.1/path") throws "Private IP ranges are not allowed"
    - assertSafeUrl("https://172.16.0.1/path") throws "Private IP ranges are not allowed"
    - assertSafeUrl("https://127.0.0.1/path") throws "Private IP ranges are not allowed"
    - assertSafeUrl("https://169.254.1.1/path") throws "Private IP ranges are not allowed"
    - assertSafeUrl("https://localhost/path") throws "Internal hostnames are not allowed"
    - assertSafeUrl("https://metadata.google.internal/path") throws "Internal hostnames are not allowed"
    - assertSafeUrl("not-a-url") throws "Invalid URL"
    - assertSafeUrl("https://example.com/valid") does not throw
    - assertSafeUrl("https://api.twitter.com/2/tweets") does not throw
  </behavior>
  <action>
    1. Create extensions/security/ package scaffold. Package name: @social/security. Follow same pattern as other extension packages.
    2. Create ssrf.guard.ts with assertSafeUrl(rawUrl: string): void function. Implementation per 08-RESEARCH.md Pattern 5: parse URL, reject non-HTTPS, check hostname against private ranges using regex (127.x, 10.x, 172.16-31.x, 192.168.x, 169.254.x) and IPv6 (::1, fc00:, fd00:). Block known internal hostnames: localhost, metadata.google.internal. Export from index.ts.
    3. Write ssrf.guard.spec.ts with all behavior cases above. Pure unit tests, no network calls.
    4. Add @social/security to pnpm-workspace.yaml if not auto-discovered.
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && pnpm test --filter @social/security 2>&1 | tail -20</automated>
  </verify>
  <done>assertSafeUrl correctly blocks all private IP ranges, non-HTTPS, and internal hostnames. Allows valid public HTTPS URLs. All 11+ test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Audit prompt injection safeguards and .env.example completeness</name>
  <files>.env.example</files>
  <action>
    1. NF1.3 AUDIT: Grep all extension packages for AI prompt construction (search for "system", "user", "messages" in files that build AI prompts). Verify user-supplied content (brief, captions) is ONLY placed in the "user" message role, never concatenated into system prompts. This was implemented in Phase 5 per decision "Brief in USER message only". Document the audit finding in the SUMMARY.
    2. NF1.5 AUDIT: Grep for any hardcoded secrets, API keys, or tokens in source code (search for patterns like 'sk-', 'Bearer ', hardcoded URLs with credentials). Confirm all sensitive values come from process.env.
    3. NF1.6: Verify .env.example includes DISABLE_REGISTRATION=true. If missing, add it under a "=== Production Settings" section.
    4. NF5.2: Audit .env.example for completeness. Ensure ALL env vars used across all extensions are documented: ENCRYPTION_KEY, MINIO_* vars, AI_* vars (OPENAI_API_KEY, ANTHROPIC_API_KEY, OLLAMA_URL), META_APP_ID, META_APP_SECRET, TWITTER_*, LINKEDIN_*. Add any missing vars with comments explaining their purpose.
    5. Add production-specific vars: NODE_ENV=production, LOG_LEVEL=info, DOMAIN=social.yourdomain.com.
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && grep -c "DISABLE_REGISTRATION" .env.example && grep -c "ENCRYPTION_KEY" .env.example && grep -c "LOG_LEVEL" .env.example</automated>
  </verify>
  <done>.env.example contains all required env vars. DISABLE_REGISTRATION=true documented. Prompt injection audit confirms user input only in user role. No hardcoded secrets found in source.</done>
</task>

</tasks>

<verification>
- SSRF tests pass: `pnpm test --filter @social/security`
- .env.example has DISABLE_REGISTRATION, ENCRYPTION_KEY, LOG_LEVEL entries
- No hardcoded secrets in source code
</verification>

<success_criteria>
- assertSafeUrl exported and all unit tests green
- .env.example is complete with all production settings documented
- Prompt injection audit confirms NF1.3 compliance
- No secrets in version control (NF1.5)
- DISABLE_REGISTRATION documented for single-user auth (NF1.6)
</success_criteria>

<output>
After completion, create `.planning/phases/08-production-hardening-deployment/08-02-SUMMARY.md`
</output>
