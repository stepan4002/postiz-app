---
phase: 08-production-hardening-deployment
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - scripts/smoke-test.sh
  - SMOKE-TEST-MANUAL.md
autonomous: true
requirements: [NF2.4, NF5.1, NF5.2, NF5.3]

must_haves:
  truths:
    - "smoke-test.sh validates /health/live and /health/ready return 200"
    - "smoke-test.sh validates SSRF protection blocks private IPs"
    - "smoke-test.sh validates graceful degradation by creating a post without AI provider (NF2.4)"
    - "smoke-test.sh exits 0 on all pass, exit 1 on any failure"
    - "Manual checklist covers OAuth connect, actual publish, metrics verification"
  artifacts:
    - path: "scripts/smoke-test.sh"
      provides: "Automated end-to-end smoke test"
      contains: "health/live"
    - path: "SMOKE-TEST-MANUAL.md"
      provides: "Manual verification checklist for steps requiring real API tokens"
      contains: "OAuth"
  key_links:
    - from: "scripts/smoke-test.sh"
      to: "/health/live"
      via: "curl assertion"
      pattern: "health/live"
    - from: "scripts/smoke-test.sh"
      to: "/health/ready"
      via: "curl assertion"
      pattern: "health/ready"
---

<objective>
Create the automated smoke test script and manual verification checklist that validate the entire system works end-to-end in production.

Purpose: Final validation gate before declaring the system production-ready. Automated script tests health, security, graceful degradation; manual checklist covers OAuth and publishing flows needing real tokens.
Output: scripts/smoke-test.sh, SMOKE-TEST-MANUAL.md
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-production-hardening-deployment/08-RESEARCH.md
@.planning/phases/08-production-hardening-deployment/08-01-SUMMARY.md
@.planning/phases/08-production-hardening-deployment/08-02-SUMMARY.md
@.planning/phases/08-production-hardening-deployment/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated smoke test script</name>
  <files>scripts/smoke-test.sh</files>
  <action>
    1. Create scripts/smoke-test.sh based on 08-RESEARCH.md Pattern 7 but expanded. Accept BASE_URL as first argument (default: https://social.yourdomain.com). Use set -euo pipefail.
    2. Define assert_http() helper: takes description, URL, expected HTTP code. Uses curl -s -o /dev/null -w "%{http_code}". Prints PASS/FAIL with counts.
    3. Define assert_json_contains() helper: takes description, URL, JSON key, expected value. Uses curl + python3 for JSON parsing.
    4. Health check section:
       - /health/live returns 200
       - /health/ready returns 200
       - /health/ready has status=ok in JSON
    5. Security assertions section:
       - SSRF: POST to a URL-accepting endpoint with http://192.168.1.1/evil, expect 4xx response
       - SSRF: POST with http://10.0.0.1/internal, expect 4xx
       - Prompt injection: POST to content generation endpoint with system prompt override attempt in brief text, expect the system to not leak system prompt (verify via response content or 4xx)
    6. NF2.4 Graceful degradation section:
       - Test that a direct post creation (without AI content generation) succeeds even when no AI provider keys are configured. This validates that the system degrades gracefully without an AI provider.
       - Use the post creation API endpoint to create a manual post (user provides their own caption text, no AI generation requested). Assert the API returns 200/201 (post created successfully).
       - Add comment: "NF2.4: Verifies system works without AI provider — manual post creation bypasses AI entirely"
    7. API basic assertions section:
       - GET /api/companies returns 200 (API is up and serving)
       - GET / returns 200 (frontend is up)
    8. Summary section: print PASSED/FAILED counts. Exit 0 if all pass, exit 1 if any fail.
    9. chmod +x scripts/smoke-test.sh.
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && test -f scripts/smoke-test.sh && bash -n scripts/smoke-test.sh && echo "SYNTAX OK" && grep -c "health/live" scripts/smoke-test.sh && grep -c "health/ready" scripts/smoke-test.sh && grep -c "NF2.4" scripts/smoke-test.sh</automated>
  </verify>
  <done>smoke-test.sh exists, has valid bash syntax, contains health check assertions, SSRF assertions, prompt injection check, and NF2.4 graceful degradation test (manual post creation without AI). Exits 0/1 based on results.</done>
</task>

<task type="auto">
  <name>Task 2: Manual smoke test checklist for OAuth and publishing flows</name>
  <files>SMOKE-TEST-MANUAL.md</files>
  <action>
    1. Create SMOKE-TEST-MANUAL.md with structured checklist sections:
    2. Section 1 - Prerequisites: system running via docker-compose.prod.yaml, operator account created, seed data loaded.
    3. Section 2 - OAuth Connect Flow:
       - [ ] Connect Instagram account via Meta Business OAuth
       - [ ] Connect Facebook page via Meta Business OAuth
       - [ ] Connect LinkedIn profile
       - [ ] Connect X (Twitter) account
       - [ ] Verify token health dashboard shows all connections as healthy
    4. Section 3 - Content Generation:
       - [ ] Upload image via media library
       - [ ] Create post with brief, select target platforms
       - [ ] Verify AI generates platform-adapted captions
       - [ ] Verify confidence scoring appears on generated content
       - [ ] If confidence < threshold, verify post appears in review queue
    5. Section 4 - Review and Scheduling:
       - [ ] Approve a post in review queue
       - [ ] Schedule approved post for future time
       - [ ] Verify post appears on scheduling calendar
    6. Section 5 - Publishing:
       - [ ] Wait for scheduled time or manually trigger
       - [ ] Verify post published to target platform
       - [ ] Check publish attempt log shows success
    7. Section 6 - Analytics:
       - [ ] Wait for T+1h analytics ingestion
       - [ ] Verify per-post metrics appear (impressions, likes, etc.)
       - [ ] Verify dashboard shows post in top performing or today's posts
    8. Section 7 - Failure Scenarios:
       - [ ] Revoke a token and verify token health alert surfaces
       - [ ] Schedule a post with invalid credentials and verify it moves to FAILED
       - [ ] Verify failed post appears in dashboard failures panel
    9. Section 8 - Graceful Degradation (NF2.4):
       - [ ] Remove or unset all AI provider env vars (OPENAI_API_KEY, ANTHROPIC_API_KEY, OLLAMA_URL)
       - [ ] Restart the system
       - [ ] Create a post with manually typed caption (no AI generation)
       - [ ] Verify the post is created, scheduled, and published successfully without AI
       - [ ] Restore AI provider env vars and restart
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && test -f SMOKE-TEST-MANUAL.md && grep -c "\[ \]" SMOKE-TEST-MANUAL.md && grep -c "NF2.4\|Graceful Degradation" SMOKE-TEST-MANUAL.md</automated>
  </verify>
  <done>SMOKE-TEST-MANUAL.md exists with 20+ checklist items covering the full core loop: connect -> generate -> review -> schedule -> publish -> metrics. Includes failure scenarios and NF2.4 graceful degradation verification.</done>
</task>

</tasks>

<verification>
- smoke-test.sh passes bash syntax check: `bash -n scripts/smoke-test.sh`
- smoke-test.sh contains health, SSRF, prompt injection, and NF2.4 graceful degradation assertions
- SMOKE-TEST-MANUAL.md covers all 8 sections including graceful degradation
</verification>

<success_criteria>
- Automated smoke test validates health endpoints, security hardening, and graceful degradation without AI (NF2.4)
- Manual checklist covers every step of the core loop requiring real API tokens
- Both artifacts ready for production deployment validation
- Phase 8 production hardening is complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-production-hardening-deployment/08-04-SUMMARY.md`
</output>
