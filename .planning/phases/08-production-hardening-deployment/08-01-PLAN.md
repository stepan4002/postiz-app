---
phase: 08-production-hardening-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/health/package.json
  - extensions/health/tsconfig.json
  - extensions/health/src/health.module.ts
  - extensions/health/src/health.controller.ts
  - extensions/health/src/indicators/database.indicator.ts
  - extensions/health/src/indicators/redis.indicator.ts
  - extensions/health/src/indicators/minio.indicator.ts
  - extensions/health/src/__tests__/health.controller.spec.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/main.ts
autonomous: true
requirements: [NF5.3, NF5.4, NF5.2]

must_haves:
  truths:
    - "GET /health/live returns 200 with { status: 'ok', timestamp }"
    - "GET /health/ready checks DB, Redis, MinIO and returns composite JSON status"
    - "All backend and worker logs emit structured JSON in production mode"
  artifacts:
    - path: "extensions/health/src/health.controller.ts"
      provides: "Health check endpoints"
      exports: ["HealthController"]
    - path: "extensions/health/src/indicators/database.indicator.ts"
      provides: "Database connectivity check"
      exports: ["DatabaseHealthIndicator"]
    - path: "extensions/health/src/indicators/redis.indicator.ts"
      provides: "Redis connectivity check"
      exports: ["RedisHealthIndicator"]
    - path: "extensions/health/src/indicators/minio.indicator.ts"
      provides: "MinIO connectivity check"
      exports: ["MinioHealthIndicator"]
    - path: "extensions/health/src/health.module.ts"
      provides: "NestJS module wiring"
      exports: ["HealthModule"]
  key_links:
    - from: "apps/backend/src/app.module.ts"
      to: "extensions/health/src/health.module.ts"
      via: "imports array"
      pattern: "HealthModule"
    - from: "apps/backend/src/main.ts"
      to: "nestjs-pino Logger"
      via: "app.useLogger"
      pattern: "app\\.useLogger"
---

<objective>
Create health check endpoints (GET /health/live and GET /health/ready) using @nestjs/terminus, and configure structured JSON logging via nestjs-pino for all backend and worker processes.

Purpose: Production monitoring requires health endpoints for Traefik routing and structured logs for log aggregation.
Output: HealthModule extension package with 3 health indicators, JSON logging configured globally.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-production-hardening-deployment/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health check extension package with @nestjs/terminus indicators</name>
  <files>extensions/health/package.json, extensions/health/tsconfig.json, extensions/health/src/health.module.ts, extensions/health/src/health.controller.ts, extensions/health/src/indicators/database.indicator.ts, extensions/health/src/indicators/redis.indicator.ts, extensions/health/src/indicators/minio.indicator.ts, extensions/health/src/__tests__/health.controller.spec.ts</files>
  <behavior>
    - GET /health/live returns 200 with { status: 'ok', timestamp: ISO string }
    - GET /health/ready returns terminus-format JSON with database, redis, minio status
    - GET /health/ready returns 503 with error details when any dependency is down
    - DatabaseHealthIndicator runs SELECT 1 via PrismaService
    - RedisHealthIndicator runs redis.ping() and checks for PONG
    - MinioHealthIndicator runs HeadBucket against configured MINIO_BUCKET
  </behavior>
  <action>
    1. Install dependencies from monorepo root: `pnpm add @nestjs/terminus nestjs-pino pino-http && pnpm add -D pino-pretty`
    2. Create extensions/health/ package scaffold following existing extension pattern (see extensions/ai-service/package.json for reference). Package name: @social/health
    3. Create DatabaseHealthIndicator extending HealthIndicator from @nestjs/terminus. Inject PrismaService, run `this.prisma.$queryRaw\`SELECT 1\``. Follow pattern from 08-RESEARCH.md Pattern 3.
    4. Create RedisHealthIndicator extending HealthIndicator. Create Redis client from process.env.REDIS_URL. Run ping(), check for PONG.
    5. Create MinioHealthIndicator extending HealthIndicator. Create S3Client with forcePathStyle: true (same pattern as extensions/media-library MinioStorage). Run HeadBucketCommand against MINIO_BUCKET env var.
    6. Create HealthController with GET /health/live (simple { status: 'ok', timestamp }) and GET /health/ready using HealthCheckService from terminus.
    7. Create HealthModule importing TerminusModule, providing all 3 indicators + PrismaService.
    8. Write health.controller.spec.ts: mock all 3 indicators, test /live returns 200, test /ready returns composite status, test /ready returns 503 when indicator throws HealthCheckError.
    9. Add @social/health to pnpm-workspace.yaml if not auto-discovered.
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && pnpm test --filter @social/health 2>&1 | tail -20</automated>
  </verify>
  <done>Health extension package exists with 3 indicators and controller. All unit tests pass. Package builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Register HealthModule and nestjs-pino in AppModule + main.ts</name>
  <files>apps/backend/src/app.module.ts, apps/backend/src/main.ts</files>
  <action>
    1. In apps/backend/src/app.module.ts: import HealthModule from @social/health and add to imports array. Import LoggerModule from nestjs-pino and add LoggerModule.forRoot({ pinoHttp: { level: process.env.LOG_LEVEL ?? 'info', transport: process.env.NODE_ENV !== 'production' ? { target: 'pino-pretty' } : undefined } }) to imports.
    2. In apps/backend/src/main.ts: import { Logger } from 'nestjs-pino'. Add `app.useLogger(app.get(Logger))` as early as possible after NestFactory.create() and before any other setup. This ensures ALL log output is structured JSON in production.
    3. If apps/orchestrator exists and has its own app.module.ts, add the same LoggerModule.forRoot() import there for worker JSON logging.
    4. Verify the app compiles: `pnpm run build` or `pnpm exec tsc --noEmit` from apps/backend.
  </action>
  <verify>
    <automated>cd "C:\Users\nikos\Desktop\work\coding\social media" && pnpm exec tsc --noEmit -p apps/backend/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>HealthModule registered in AppModule. nestjs-pino Logger configured globally. App compiles without type errors. GET /health/live and /health/ready routes are registered.</done>
</task>

</tasks>

<verification>
- Health unit tests pass: `pnpm test --filter @social/health`
- Backend compiles: `pnpm exec tsc --noEmit -p apps/backend/tsconfig.json`
- No regressions: `pnpm test` (full suite)
</verification>

<success_criteria>
- GET /health/live endpoint exists and returns { status: 'ok', timestamp }
- GET /health/ready endpoint checks DB, Redis, MinIO and returns @nestjs/terminus format JSON
- All backend logs emit structured JSON when NODE_ENV=production
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-production-hardening-deployment/08-01-SUMMARY.md`
</output>
