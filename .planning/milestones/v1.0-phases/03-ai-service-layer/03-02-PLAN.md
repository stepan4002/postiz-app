---
phase: 03-ai-service-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - extensions/ai-service/src/providers/openai.provider.ts
  - extensions/ai-service/src/providers/anthropic.provider.ts
  - extensions/ai-service/src/providers/ollama.provider.ts
  - extensions/ai-service/src/__tests__/openai.provider.spec.ts
  - extensions/ai-service/src/__tests__/anthropic.provider.spec.ts
  - extensions/ai-service/src/__tests__/ollama.provider.spec.ts
  - extensions/ai-service/src/index.ts
autonomous: true
requirements:
  - R4.2

must_haves:
  truths:
    - "OpenAI provider calls chat.completions.parse with zodResponseFormat and returns typed AICallResult with usage"
    - "Anthropic provider calls messages.parse with zodOutputFormat, extracts system message from array into top-level system param"
    - "Ollama provider calls HTTP POST to /api/chat with JSON schema format and parses response"
    - "All three providers return AICallResult<T> with inputTokens, outputTokens, estimatedCostUsd"
    - "OpenAI and Anthropic providers support analyzeImage; Ollama supports it only if vision model configured"
    - "Providers instantiate SDK clients in constructor (not module-level) for testability"
  artifacts:
    - path: "extensions/ai-service/src/providers/openai.provider.ts"
      provides: "OpenAIProvider implementing IAIProvider"
      exports: ["OpenAIProvider"]
      min_lines: 60
    - path: "extensions/ai-service/src/providers/anthropic.provider.ts"
      provides: "AnthropicProvider implementing IAIProvider"
      exports: ["AnthropicProvider"]
      min_lines: 60
    - path: "extensions/ai-service/src/providers/ollama.provider.ts"
      provides: "OllamaProvider implementing IAIProvider"
      exports: ["OllamaProvider"]
      min_lines: 50
  key_links:
    - from: "extensions/ai-service/src/providers/openai.provider.ts"
      to: "openai SDK"
      via: "new OpenAI() in constructor, client.chat.completions.parse()"
      pattern: "zodResponseFormat"
    - from: "extensions/ai-service/src/providers/anthropic.provider.ts"
      to: "@anthropic-ai/sdk"
      via: "new Anthropic() in constructor, client.messages.parse()"
      pattern: "zodOutputFormat"
    - from: "extensions/ai-service/src/providers/ollama.provider.ts"
      to: "Ollama HTTP API"
      via: "native fetch to OLLAMA_BASE_URL/api/chat"
      pattern: "fetch.*api/chat"
    - from: "all providers"
      to: "extensions/ai-service/src/cost/model-costs.ts"
      via: "calculateCostUsd import"
      pattern: "calculateCostUsd"
---

<objective>
Implement the three AI provider classes: OpenAI (GPT-4o, GPT-4o-mini), Anthropic (Claude Sonnet, Haiku), and Ollama (local). Each implements the IAIProvider interface from Plan 01 and uses direct SDK calls (not LangChain) for clean access to usage metadata needed by the cost tracking system.

Purpose: R4.2 requires three provider implementations. Direct SDK calls (vs LangChain) give reliable token counts for cost tracking and avoid version coupling. Coexists with upstream OpenaiService (no imports from @gitroom/nestjs-libraries/openai).

Output: Three tested provider implementations ready for the router (Plan 04) to select from.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-service-layer/03-RESEARCH.md
@.planning/phases/03-ai-service-layer/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From extensions/ai-service/src/interface/ai-service.interface.ts:
```typescript
export type AITaskType =
  | 'generateCaption'
  | 'generateHashtags'
  | 'adaptForPlatform'
  | 'analyzeImage'
  | 'scoreContent';

export interface AIMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

export interface AICallContext {
  companyId: string;
  brandId?: string;
  postId?: string;
  taskType: AITaskType;
}

export interface AIUsage {
  inputTokens: number;
  outputTokens: number;
  estimatedCostUsd: number;
}

export interface AICallResult<T> {
  output: T;
  usage: AIUsage;
  model: string;
  provider: string;
}

export interface IAIProvider {
  readonly providerName: string;
  readonly supportedTaskTypes: AITaskType[];
  chat<T>(messages: AIMessage[], schema: z.ZodType<T>, model: string): Promise<AICallResult<T>>;
  analyzeImage<T>(imageUrl: string, prompt: string, schema: z.ZodType<T>, model: string): Promise<AICallResult<T>>;
}
```

From extensions/ai-service/src/cost/model-costs.ts:
```typescript
export const MODEL_COSTS: Record<string, { inputPer1M: number; outputPer1M: number }>;
export function calculateCostUsd(model: string, inputTokens: number, outputTokens: number): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAI and Anthropic provider implementations with tests</name>
  <files>
    extensions/ai-service/src/providers/openai.provider.ts,
    extensions/ai-service/src/providers/anthropic.provider.ts,
    extensions/ai-service/src/__tests__/openai.provider.spec.ts,
    extensions/ai-service/src/__tests__/anthropic.provider.spec.ts
  </files>
  <behavior>
    OpenAI:
    - chat() calls client.chat.completions.parse with zodResponseFormat, returns parsed output + usage
    - analyzeImage() sends image_url content part in user message, returns parsed output + usage
    - Usage maps: prompt_tokens -> inputTokens, completion_tokens -> outputTokens
    - estimatedCostUsd uses calculateCostUsd from model-costs
    - providerName is 'openai'
    - supportedTaskTypes includes all 5 task types (OpenAI supports everything including vision)
    - Client instantiated in constructor from process.env.OPENAI_API_KEY
    - Throws clear error if OPENAI_API_KEY not set

    Anthropic:
    - chat() calls client.messages.parse with zodOutputFormat in output_config.format
    - System message extracted from messages array and passed as top-level `system` param (Pitfall 1)
    - User/assistant messages passed in messages array (system filtered out)
    - analyzeImage() sends image as base64 source in user message content array
    - Usage maps: input_tokens -> inputTokens, output_tokens -> outputTokens
    - providerName is 'anthropic'
    - supportedTaskTypes includes all 5 task types
    - Client instantiated in constructor from process.env.ANTHROPIC_API_KEY
    - Throws clear error if ANTHROPIC_API_KEY not set
  </behavior>
  <action>
    1. Create `extensions/ai-service/src/providers/openai.provider.ts`:
       - @Injectable() class OpenAIProvider implements IAIProvider
       - Constructor: `this.client = new OpenAI({ apiKey: process.env.OPENAI_API_KEY })` — NOT module-level (Pitfall 6). If OPENAI_API_KEY is undefined, do NOT throw at construction — allow lazy failure when actually called (provider might not be selected).
       - providerName = 'openai'
       - supportedTaskTypes = all 5 AITaskType values
       - `async chat<T>(messages, schema, model)`:
         - Call `this.client.chat.completions.parse({ model, messages, response_format: zodResponseFormat(schema, 'output') })`
         - Extract usage from completion.usage
         - Return AICallResult with parsed output, model, provider, and usage (using calculateCostUsd)
       - `async analyzeImage<T>(imageUrl, prompt, schema, model)`:
         - Build messages with user message containing image_url content part: `[{ type: 'text', text: prompt }, { type: 'image_url', image_url: { url: imageUrl } }]`
         - Call chat.completions.parse with the multimodal message
         - Return AICallResult same as chat
       - Import zodResponseFormat from 'openai/helpers/zod' (existing pattern in codebase)
       - Import calculateCostUsd from '../cost/model-costs'

    2. Create `extensions/ai-service/src/providers/anthropic.provider.ts`:
       - @Injectable() class AnthropicProvider implements IAIProvider
       - Constructor: `this.client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })`
       - providerName = 'anthropic'
       - supportedTaskTypes = all 5 AITaskType values
       - `async chat<T>(messages, schema, model)`:
         - Extract system message: `const systemMsg = messages.find(m => m.role === 'system')?.content`
         - Filter non-system messages: `const userMessages = messages.filter(m => m.role !== 'system')`
         - Call `this.client.messages.parse({ model, max_tokens: 4096, system: systemMsg, messages: userMessages, output_config: { format: zodOutputFormat(schema) } })`
         - Extract usage from response.usage (input_tokens, output_tokens)
         - Return AICallResult with response.parsed_output, model, provider, usage
       - `async analyzeImage<T>(imageUrl, prompt, schema, model)`:
         - For image analysis, Anthropic expects base64 source in content blocks
         - Build user message with image content block: `{ type: 'image', source: { type: 'url', url: imageUrl } }` (Anthropic supports URL source for images)
         - Call messages.parse with the multimodal user message
         - Return AICallResult
       - Import zodOutputFormat from '@anthropic-ai/sdk/helpers/zod'
       - Import calculateCostUsd from '../cost/model-costs'

    3. Create `extensions/ai-service/src/__tests__/openai.provider.spec.ts`:
       - Mock the openai module: jest.mock('openai') — mock chat.completions.parse to return a fixture with usage object
       - Test: chat() returns correct AICallResult with mapped usage
       - Test: chat() calls parse with zodResponseFormat
       - Test: analyzeImage() includes image_url in message content
       - Test: usage includes estimatedCostUsd computed from calculateCostUsd
       - Set process.env.OPENAI_API_KEY = 'test-key' in beforeAll

    4. Create `extensions/ai-service/src/__tests__/anthropic.provider.spec.ts`:
       - Mock @anthropic-ai/sdk: jest.mock('@anthropic-ai/sdk') — mock messages.parse to return fixture with usage
       - Test: chat() extracts system message from array to top-level param
       - Test: chat() filters system from messages array
       - Test: chat() returns correct AICallResult with mapped usage
       - Test: analyzeImage() includes image content block
       - Test: usage includes estimatedCostUsd
       - Set process.env.ANTHROPIC_API_KEY = 'test-key' in beforeAll
  </action>
  <verify>
    <automated>cd extensions/ai-service && pnpm test -- openai.provider && pnpm test -- anthropic.provider</automated>
  </verify>
  <done>
    - OpenAI provider calls chat.completions.parse with zodResponseFormat, returns typed AICallResult
    - Anthropic provider calls messages.parse with zodOutputFormat, system message extracted to top-level param
    - Both providers return correct usage (inputTokens, outputTokens, estimatedCostUsd)
    - Both providers support analyzeImage with multimodal messages
    - All openai.provider.spec.ts and anthropic.provider.spec.ts tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ollama provider implementation with tests and barrel exports update</name>
  <files>
    extensions/ai-service/src/providers/ollama.provider.ts,
    extensions/ai-service/src/__tests__/ollama.provider.spec.ts,
    extensions/ai-service/src/index.ts
  </files>
  <behavior>
    - chat() sends POST to {OLLAMA_BASE_URL}/api/chat with model, messages, stream:false, format:jsonSchema
    - chat() parses response JSON, validates with Zod schema, returns AICallResult
    - Token counts from response.prompt_eval_count and response.eval_count (Pitfall 2)
    - Falls back to character-based estimation if token fields are 0 or missing
    - providerName is 'ollama'
    - supportedTaskTypes excludes 'analyzeImage' by default (unless OLLAMA_VISION_MODEL is set)
    - analyzeImage() throws UnsupportedOperationError if vision not configured
    - OLLAMA_BASE_URL defaults to 'http://localhost:11434' if not set
    - Graceful error on connection failure (network error, Ollama not running)
  </behavior>
  <action>
    1. Create `extensions/ai-service/src/providers/ollama.provider.ts`:
       - @Injectable() class OllamaProvider implements IAIProvider
       - Constructor:
         - `this.baseUrl = process.env.OLLAMA_BASE_URL || 'http://localhost:11434'`
         - `this.visionModel = process.env.OLLAMA_VISION_MODEL || null`
       - providerName = 'ollama'
       - get supportedTaskTypes: return all 5 if visionModel is set; otherwise exclude 'analyzeImage'
       - `async chat<T>(messages, schema, model)`:
         - Convert Zod schema to JSON Schema using `zodToJsonSchema` from 'zod-to-json-schema'
         - POST to `${this.baseUrl}/api/chat` with body:
           `{ model, messages: messages.map(m => ({ role: m.role, content: m.content })), stream: false, format: jsonSchema }`
         - Parse response JSON: `const data = await response.json()`
         - Extract content: `data.message.content` — parse as JSON, validate with `schema.parse()`
         - Token counts: `data.prompt_eval_count ?? 0` (input), `data.eval_count ?? 0` (output)
         - If both are 0, estimate: Math.ceil(JSON.stringify(messages).length / 4) for input, Math.ceil(data.message.content.length / 4) for output
         - Return AICallResult with calculateCostUsd (will be 0 for ollama)
       - `async analyzeImage<T>(imageUrl, prompt, schema, model)`:
         - If !this.visionModel, throw new Error('Ollama vision not configured. Set OLLAMA_VISION_MODEL env var.')
         - Use the visionModel (not the passed model param) for image tasks
         - Ollama accepts images as base64 in the `images` field of a message
         - Fetch the image URL, convert to base64, pass in messages[].images array
         - Call chat internally with the vision model
       - Handle fetch errors: wrap in try/catch, rethrow with descriptive message including baseUrl
       - Import zodToJsonSchema from 'zod-to-json-schema'

    2. Create `extensions/ai-service/src/__tests__/ollama.provider.spec.ts`:
       - Mock global fetch: `jest.spyOn(global, 'fetch')` to return mock responses
       - Test: chat() sends POST to correct URL with correct body shape
       - Test: chat() parses response.message.content as JSON and validates with schema
       - Test: token counts extracted from prompt_eval_count/eval_count
       - Test: fallback estimation when token fields are 0
       - Test: supportedTaskTypes excludes analyzeImage when OLLAMA_VISION_MODEL not set
       - Test: supportedTaskTypes includes analyzeImage when OLLAMA_VISION_MODEL is set
       - Test: analyzeImage() throws when vision not configured
       - Test: connection error produces descriptive error message

    3. Update `extensions/ai-service/src/index.ts` barrel:
       - Add exports for OpenAIProvider, AnthropicProvider, OllamaProvider
       - Ensure all provider classes and the IAIProvider interface are accessible from the package root
  </action>
  <verify>
    <automated>cd extensions/ai-service && pnpm test -- ollama.provider</automated>
  </verify>
  <done>
    - Ollama provider sends HTTP POST to /api/chat with JSON schema format
    - Token counts extracted from Ollama-specific fields with character-based fallback
    - Vision gated behind OLLAMA_VISION_MODEL env var
    - Connection errors wrapped with descriptive messages
    - All ollama.provider.spec.ts tests pass
    - All three providers exported from index.ts barrel
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @social/ai-service test` passes all tests (openai, anthropic, ollama providers + prior tests)
- All three providers implement IAIProvider interface (providerName, supportedTaskTypes, chat, analyzeImage)
- No imports from @gitroom/nestjs-libraries/openai (upstream OpenaiService is untouched)
- SDK clients instantiated in constructors, not at module level
- Anthropic system message handling: extracted from array, passed as top-level param
</verification>

<success_criteria>
Three AI provider implementations tested with mocked SDKs. Each returns typed AICallResult with token counts and cost. Ready for Plan 04 to wire into the provider router.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-service-layer/03-02-SUMMARY.md`
</output>
