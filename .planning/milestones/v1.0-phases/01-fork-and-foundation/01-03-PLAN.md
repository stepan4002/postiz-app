---
phase: 01-fork-and-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - extensions/multi-company/src/company/company.module.ts
  - extensions/multi-company/src/company/company.service.ts
  - extensions/multi-company/src/company/company.controller.ts
  - extensions/multi-company/src/company/dto/create-company.dto.ts
  - extensions/multi-company/src/company/dto/update-company.dto.ts
  - extensions/multi-company/src/brand/brand.module.ts
  - extensions/multi-company/src/brand/brand.service.ts
  - extensions/multi-company/src/brand/brand.controller.ts
  - extensions/multi-company/src/brand/dto/create-brand.dto.ts
  - extensions/multi-company/src/brand/dto/update-brand.dto.ts
  - extensions/multi-company/src/brand-voice/brand-voice.module.ts
  - extensions/multi-company/src/brand-voice/brand-voice.service.ts
  - extensions/multi-company/src/brand-voice/brand-voice.controller.ts
  - extensions/multi-company/src/brand-voice/dto/upsert-brand-voice.dto.ts
  - extensions/multi-company/src/multi-company.module.ts
  - extensions/multi-company/src/index.ts
  - extensions/seed/seed.ts
  - apps/backend/src/app/app.module.ts
  - DIVERGENCE.md
autonomous: true
requirements: [R2.7, R2.8]

must_haves:
  truths:
    - "Company CRUD API responds at /api/companies with create, read, update, delete operations"
    - "Brand CRUD API responds at /api/companies/:companySlug/brands"
    - "BrandVoice upsert API responds at /api/companies/:companySlug/brands/:brandSlug/voice"
    - "Seed script creates 3 test companies with brands, brand voices, and mock social accounts"
    - "Seed script is idempotent -- running it twice produces the same result"
  artifacts:
    - path: "extensions/multi-company/src/company/company.controller.ts"
      provides: "Company CRUD REST endpoints"
      exports: ["CompanyController"]
    - path: "extensions/multi-company/src/brand/brand.controller.ts"
      provides: "Brand CRUD REST endpoints scoped to company"
      exports: ["BrandController"]
    - path: "extensions/multi-company/src/brand-voice/brand-voice.controller.ts"
      provides: "BrandVoice upsert endpoint scoped to brand"
      exports: ["BrandVoiceController"]
    - path: "extensions/seed/seed.ts"
      provides: "Idempotent seed script with 3 companies"
      contains: "upsert"
  key_links:
    - from: "extensions/multi-company/src/company/company.service.ts"
      to: "libraries/nestjs-libraries/src/database/prisma/prisma.service.ts"
      via: "Prisma dependency injection"
      pattern: "PrismaService"
    - from: "extensions/multi-company/src/multi-company.module.ts"
      to: "apps/backend/src/app/app.module.ts"
      via: "NestJS module import"
      pattern: "MultiCompanyModule"
    - from: "extensions/seed/seed.ts"
      to: "libraries/nestjs-libraries/src/database/prisma/schema.prisma"
      via: "Prisma client generated from schema"
      pattern: "prisma\\.company\\.upsert"
---

<objective>
Build the Company/Brand/BrandVoice CRUD API and create an idempotent seed script with 3 test companies.

Purpose: Deliver the working backend API that the company switcher UI will consume and populate the database with realistic test data for development and testing.

Output: REST API for company management and seed script with 3 companies.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-CONTEXT.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@.planning/phases/01-fork-and-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types from Plan 02 that this plan consumes -->

From schema.prisma (created in Plan 02):
- model Company { id, name, slug, timezone, defaultLanguage, industry, website, logo, notes, brands[] }
- model Brand { id, companyId, name, slug, logo, description, company, brandVoice?, socialAccounts[] }
- model BrandVoice { id, brandId, tone[], targetAudience, preferredHashtags[], blacklistedWords[], samplePosts[], language, notes }
- model SocialAccount { id, brandId, platform, externalId, displayName, integrationId }

From @social/company-context (created in Plan 02):
- CompanyContextModule — registers CLS + middleware
- COMPANY_PRISMA — injection token for company-scoped Prisma client
- prismaWithCompany(prisma, companyId) — returns scoped Prisma client

From Postiz:
- PrismaService from '@gitroom/nestjs-libraries/database/prisma/prisma.service'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Company CRUD module and DTOs</name>
  <files>
    extensions/multi-company/src/company/company.module.ts,
    extensions/multi-company/src/company/company.service.ts,
    extensions/multi-company/src/company/company.controller.ts,
    extensions/multi-company/src/company/dto/create-company.dto.ts,
    extensions/multi-company/src/company/dto/update-company.dto.ts
  </files>
  <action>
    1. **CreateCompanyDto** (`extensions/multi-company/src/company/dto/create-company.dto.ts`):
       - name (required, 2-100 chars), slug (required, lowercase alphanumeric+hyphens), timezone (optional, default UTC), defaultLanguage (optional, default en), industry, website, logo, notes — all optional strings

    2. **UpdateCompanyDto** (`extensions/multi-company/src/company/dto/update-company.dto.ts`):
       - All fields optional (partial update)

    3. **CompanyService** (`extensions/multi-company/src/company/company.service.ts`):
       - Inject `PrismaService` from `@gitroom/nestjs-libraries/database/prisma/prisma.service`
       - `findAll()`: return all companies with brands (include brandVoice, socialAccounts), ordered by name
       - `findBySlug(slug: string)`: return single company by slug with full include tree
       - `findById(id: string)`: return single company by id
       - `create(dto: CreateCompanyDto)`: create company, validate slug uniqueness
       - `update(id: string, dto: UpdateCompanyDto)`: partial update
       - `delete(id: string)`: delete company (cascade deletes brands, voices, accounts)

    4. **CompanyController** (`extensions/multi-company/src/company/company.controller.ts`):
       - `GET /api/companies` — list all companies
       - `GET /api/companies/:slug` — get company by slug
       - `POST /api/companies` — create company (body: CreateCompanyDto)
       - `PUT /api/companies/:id` — update company
       - `DELETE /api/companies/:id` — delete company

    5. **CompanyModule** (`extensions/multi-company/src/company/company.module.ts`):
       - Provides CompanyService
       - Registers CompanyController
  </action>
  <verify>
    <automated>npx tsc --noEmit -p extensions/multi-company/tsconfig.json 2>&1 | head -20 && echo "Company module compiles"</automated>
  </verify>
  <done>
    - Company CRUD controller and service exist with full REST endpoints
    - DTOs validate input with proper constraints
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Brand/BrandVoice CRUD, MultiCompanyModule, barrel exports, and seed script</name>
  <files>
    extensions/multi-company/src/brand/brand.module.ts,
    extensions/multi-company/src/brand/brand.service.ts,
    extensions/multi-company/src/brand/brand.controller.ts,
    extensions/multi-company/src/brand/dto/create-brand.dto.ts,
    extensions/multi-company/src/brand/dto/update-brand.dto.ts,
    extensions/multi-company/src/brand-voice/brand-voice.module.ts,
    extensions/multi-company/src/brand-voice/brand-voice.service.ts,
    extensions/multi-company/src/brand-voice/brand-voice.controller.ts,
    extensions/multi-company/src/brand-voice/dto/upsert-brand-voice.dto.ts,
    extensions/multi-company/src/multi-company.module.ts,
    extensions/multi-company/src/index.ts,
    extensions/seed/seed.ts,
    apps/backend/src/app/app.module.ts,
    DIVERGENCE.md
  </files>
  <action>
    **Brand CRUD:**

    1. **CreateBrandDto/UpdateBrandDto**: name (required), slug (required, lowercase alphanumeric+hyphens), logo (optional), description (optional)

    2. **BrandService** (`extensions/multi-company/src/brand/brand.service.ts`):
       - All operations scoped by companyId (passed as parameter, NOT from CLS — services should be explicit)
       - `findAllByCompany(companyId: string)`: brands with brandVoice and socialAccounts
       - `findBySlug(companyId: string, slug: string)`: single brand
       - `create(companyId: string, dto: CreateBrandDto)`: create brand linked to company
       - `update(id: string, dto: UpdateBrandDto)`: partial update
       - `delete(id: string)`: cascade delete

    3. **BrandController** (`extensions/multi-company/src/brand/brand.controller.ts`):
       - Nested under company: `/api/companies/:companySlug/brands`
       - Resolves companySlug to companyId in each handler
       - `GET /api/companies/:companySlug/brands` — list brands
       - `POST /api/companies/:companySlug/brands` — create brand
       - `PUT /api/companies/:companySlug/brands/:id` — update brand
       - `DELETE /api/companies/:companySlug/brands/:id` — delete brand

    **BrandVoice CRUD:**

    4. **UpsertBrandVoiceDto**: tone (string[]), targetAudience (string?), preferredHashtags (string[]), blacklistedWords (string[]), samplePosts (string[]), language (string, default "en"), notes (string?)

    5. **BrandVoiceService** (`extensions/multi-company/src/brand-voice/brand-voice.service.ts`):
       - `findByBrand(brandId: string)`: get brand voice
       - `upsert(brandId: string, dto: UpsertBrandVoiceDto)`: create or update brand voice

    6. **BrandVoiceController** (`extensions/multi-company/src/brand-voice/brand-voice.controller.ts`):
       - `GET /api/companies/:companySlug/brands/:brandSlug/voice` — get brand voice
       - `PUT /api/companies/:companySlug/brands/:brandSlug/voice` — upsert brand voice

    **Wiring:**

    7. **MultiCompanyModule** (`extensions/multi-company/src/multi-company.module.ts`):
       - Imports CompanyModule, BrandModule, BrandVoiceModule
       - Exports all three
       - Register in `apps/backend/src/app/app.module.ts` (update DIVERGENCE.md)

    8. Update `extensions/multi-company/src/index.ts` to barrel-export MultiCompanyModule and all services

    **Seed Script:**

    9. Create `extensions/seed/seed.ts` — idempotent seed using Prisma `upsert`:

    **Company 1: Verde Kitchen** (restaurant, 1 brand)
    - slug: `verde-kitchen`, industry: food-beverage, timezone: Europe/Athens, language: el
    - Brand: "Verde Kitchen" (slug: verde-main)
    - BrandVoice: tone [warm, friendly, appetizing], audience "Families and food lovers aged 25-55", hashtags [#MediterraneanFood, #FreshIngredients, #FamilyDining], blacklist [cheap, fast food, greasy], language: el, notes about fresh seasonal ingredients
    - SocialAccounts: instagram (@verde.kitchen), facebook (Verde Kitchen)

    **Company 2: Nexus AI** (tech startup, 1 brand)
    - slug: `nexus-ai`, industry: technology, timezone: UTC, language: en
    - Brand: "Nexus AI" (slug: nexus-main)
    - BrandVoice: tone [professional, innovative, clear], audience "SMB operations teams and CTOs", hashtags [#AIAutomation, #OperationsAI, #FutureOfWork], blacklist [hype, revolutionary, disruptive], language: en
    - SocialAccounts: instagram, linkedin, x

    **Company 3: Aura Fashion** (fashion brand, 2 brands)
    - slug: `aura-fashion`, industry: fashion, timezone: Europe/London, language: en
    - Brand 1: "Aura Womenswear" (slug: aura-womenswear)
      - BrandVoice: tone [elegant, aspirational, empowering], audience "Women 25-45, urban professionals"
      - SocialAccounts: instagram, facebook
    - Brand 2: "Aura Active" (slug: aura-active)
      - BrandVoice: tone [energetic, motivating, inclusive], audience "Fitness-minded women 20-40"
      - SocialAccounts: instagram

    Each entity uses `upsert` with the unique constraint as the where clause (slug for Company, companyId+slug for Brand, brandId for BrandVoice, brandId+platform for SocialAccount).

    Add to root `package.json`: `"prisma:seed": "tsx extensions/seed/seed.ts"`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p extensions/multi-company/tsconfig.json 2>&1 | head -20 && echo "TypeScript compiles"</automated>
  </verify>
  <done>
    - Brand/BrandVoice CRUD controllers and services exist with full REST endpoints
    - MultiCompanyModule registered in AppModule
    - Seed script creates 3 companies (Verde Kitchen, Nexus AI, Aura Fashion) with brands, voices, and mock social accounts
    - Seed script is idempotent (uses upserts on unique constraints)
    - DIVERGENCE.md updated for app.module.ts MultiCompanyModule import
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p extensions/multi-company/tsconfig.json` compiles without errors
- `tsx extensions/seed/seed.ts` — seed runs without errors, can be run twice idempotently
- `grep "MultiCompanyModule" apps/backend/src/app/app.module.ts` returns a match
</verification>

<success_criteria>
- Company, Brand, BrandVoice REST APIs are functional with proper CRUD operations
- Seed script populates 3 companies with realistic data and is re-runnable
- MultiCompanyModule registered in AppModule with DIVERGENCE.md entry
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-03-SUMMARY.md`
</output>
