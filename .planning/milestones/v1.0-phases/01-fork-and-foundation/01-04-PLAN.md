---
phase: 01-fork-and-foundation
plan: 04
type: tdd
wave: 3
depends_on: ["01-03"]
files_modified:
  - extensions/multi-company/src/__tests__/company-isolation.spec.ts
  - extensions/multi-company/src/__tests__/company.service.spec.ts
  - extensions/multi-company/jest.config.ts
  - extensions/multi-company/tsconfig.spec.json
autonomous: true
requirements: [R2.8]

must_haves:
  truths:
    - "Company A data is not visible when querying in Company B context"
    - "Cascade delete removes all child entities (brands, voices, accounts)"
    - "Company slug uniqueness is enforced at database level"
  artifacts:
    - path: "extensions/multi-company/src/__tests__/company-isolation.spec.ts"
      provides: "Automated data isolation tests"
      contains: "Company Data Isolation"
    - path: "extensions/multi-company/src/__tests__/company.service.spec.ts"
      provides: "Company CRUD unit tests"
      contains: "CompanyService"
    - path: "extensions/multi-company/jest.config.ts"
      provides: "Jest configuration for multi-company extension"
      contains: "ts-jest"
  key_links:
    - from: "extensions/multi-company/src/__tests__/company-isolation.spec.ts"
      to: "extensions/multi-company/src/company/company.service.ts"
      via: "Tests service methods for cross-company isolation"
      pattern: "CompanyService|BrandService"
    - from: "extensions/multi-company/src/__tests__/company.service.spec.ts"
      to: "extensions/multi-company/src/company/company.service.ts"
      via: "Tests CRUD operations and constraints"
      pattern: "CompanyService"
---

<objective>
Write and run automated company data isolation tests proving cross-company data leakage is impossible, plus CRUD operation tests for Company service.

Purpose: Prove the data isolation contract with automated tests -- this is the critical safety net ensuring Company A can never see Company B's data.

Output: Passing isolation test suite and CRUD test suite.
</objective>

<execution_context>
@C:/Users/nikos/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nikos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-CONTEXT.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@.planning/phases/01-fork-and-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Services and types from Plan 03 that tests will exercise -->

From @social/multi-company (created in Plan 03):
- CompanyService: findAll(), findBySlug(slug), findById(id), create(dto), update(id, dto), delete(id)
- BrandService: findAllByCompany(companyId), findBySlug(companyId, slug), create(companyId, dto), update(id, dto), delete(id)
- BrandVoiceService: findByBrand(brandId), upsert(brandId, dto)

From schema.prisma (created in Plan 02):
- Company: slug is @unique
- Brand: @@unique([companyId, slug])
- BrandVoice: brandId is @unique
- SocialAccount: @@unique([brandId, platform])
- All relations use onDelete: Cascade
</interfaces>
</context>

<feature>
  <name>Company Data Isolation</name>
  <files>
    extensions/multi-company/src/__tests__/company-isolation.spec.ts,
    extensions/multi-company/src/__tests__/company.service.spec.ts,
    extensions/multi-company/jest.config.ts,
    extensions/multi-company/tsconfig.spec.json
  </files>
  <behavior>
    - Test 1: Creating a brand for Company A does not make it visible when querying brands for Company B
    - Test 2: Querying brands for Company B returns empty array when only Company A has brands
    - Test 3: Company findAll returns all companies (not scoped -- companies are top-level)
    - Test 4: Brand findAllByCompany with Company A ID only returns Company A brands, not Company B
    - Test 5: BrandVoice for Company A brand is not accessible via Company B brand query
    - Test 6: SocialAccount for Company A brand not visible in Company B brand context
    - Test 7: Deleting Company cascades to delete its brands, brand voices, and social accounts
    - Test 8: Company slug uniqueness is enforced (duplicate slug throws)
  </behavior>
  <implementation>
    1. Create `extensions/multi-company/jest.config.ts`:
       - Use NX Jest preset or configure manually with ts-jest
       - Set moduleNameMapper to resolve `@gitroom/*` and `@social/*` paths
       - Configure testMatch for `**/__tests__/**/*.spec.ts`

    2. Create `extensions/multi-company/tsconfig.spec.json`:
       - Extends package tsconfig
       - Includes test files
       - Adds jest types

    3. Create `extensions/multi-company/src/__tests__/company-isolation.spec.ts`:
       - Uses NestJS Test module to create a testing context with PrismaService
       - beforeAll: create two test companies (isolation-test-a, isolation-test-b) with brands, brandVoice, and socialAccounts for company A only
       - Run all isolation behavior tests listed above
       - afterAll: clean up test data (delete both companies -- cascade handles children)

    4. Create `extensions/multi-company/src/__tests__/company.service.spec.ts`:
       - Tests CompanyService CRUD operations
       - Tests slug uniqueness constraint
       - Tests cascade delete behavior

    5. Run the test suite:
       ```bash
       npx nx test multi-company --testPathPattern=company-isolation
       ```

    IMPORTANT: These tests need a running PostgreSQL database. They are integration tests that use the real Prisma client against a test database. Ensure Docker dev environment is up before running.
  </implementation>
</feature>

<verification>
- `npx nx test multi-company` — all tests pass
- All 8+ test cases green
- Tests clean up after themselves (no leftover test data)
</verification>

<success_criteria>
- Jest config exists for multi-company extension
- Company isolation tests pass: Company A data invisible to Company B queries
- Company CRUD tests pass: create, read, update, delete, slug uniqueness, cascade delete
- All 8+ test cases green
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-04-SUMMARY.md`
</output>
